server {
    listen 80;

    location /logos/ {
        alias /usr/share/nginx/html/logos/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        add_header Access-Control-Allow-Origin "*";
    }
}
