{
  "name": "@anstoss/admin",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "mkdir -p dist && cp -R src/. dist/",
    "lint": "node --check src/main.js && node --check scripts/dev-server.mjs && sh -n docker-entrypoint.d/10-admin-basic-auth.sh",
    "typecheck": "node --check src/main.js && node --check scripts/dev-server.mjs",
    "test": "node scripts/smoke-test.mjs"
  }
}
