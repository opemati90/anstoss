appId: com.renuirug.anstoss
---
# Smoke test of the rebuilt unified sign-in screen (phone/email + inline OTP).
# launch.yaml already lands on this screen and waits for "Send code".
- runFlow: subflows/launch.yaml
- assertVisible: "Welcome back"
- assertVisible: "Send code"
- assertVisible: ".*Create account.*"
