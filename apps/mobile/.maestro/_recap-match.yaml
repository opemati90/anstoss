appId: com.renuirug.anstoss
---
# Capture the live match screen via the home "Open live match" CTA, which
# carries the seeded match id (the raw deeplink has none → error state).
- runFlow: subflows/launch.yaml
- runFlow:
    file: subflows/deeplink.yaml
    env:
      URL: "anstoss://e2e?scenario=player"
- extendedWaitUntil:
    visible: "Open live match"
    timeout: 15000
- tapOn: "Open live match"
- waitForAnimationToEnd:
    timeout: 5000
- takeScreenshot: "03-player-match"
